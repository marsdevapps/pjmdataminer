package com.marsdev.pjm.models

import tornadofx.*
import javax.json.JsonObject

class PNodeSearch : JsonModel {

    override fun updateModel(json: JsonObject) {
        with(json) {
        }
    }
}

